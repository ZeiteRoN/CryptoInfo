<Window
    Height="450"
    Title="Currency Info"
    Width="800"
    mc:Ignorable="d"
    x:Class="CryptoInfo.MVVM.Views.SearchWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CryptoInfo.MVVM.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="20">
        <TextBlock
            FontSize="24"
            HorizontalAlignment="Center"
            Text="Search Cryptocurrency" />

        <TextBox
            Margin="0,10"
            Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
            Width="200" />
        <Button
            Command="{Binding SearchCommand}"
            Content="Search"
            Width="100" />

        <StackPanel DataContext="{Binding SelectedCryptocurrency}" Visibility="{Binding SelectedCryptocurrency}">
            <TextBlock FontWeight="Bold" Text="Name: " />
            <TextBlock Text="{Binding name}" />
            <TextBlock FontWeight="Bold" Text="Price (USD): " />
            <TextBlock Text="{Binding priceUsd, StringFormat={}{0:C}}" />
        </StackPanel>

        <Button
            Click="returnToMainWindow_Click"
            Content="Повернутися"
            Margin="0,10"
            Width="100" />
    </StackPanel>
</Window>
